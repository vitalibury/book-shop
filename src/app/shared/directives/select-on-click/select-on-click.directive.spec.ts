import { SelectOnClickDirective } from './select-on-click.directive';

describe('SelectOnClickDirective', () => {
    it('should create an instance', () => {
        const directive = new SelectOnClickDirective();
        expect(directive).toBeTruthy();
    });
});
